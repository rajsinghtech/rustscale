[package]
name = "rustscale-filter"
description = "Stateful packet filter for rustscale — port of Tailscale's wgengine/filter"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
rustscale-packet = { path = "../packet", version = "0.1.0" }
rustscale-tailcfg = { path = "../tailcfg", version = "0.1.0" }
thiserror.workspace = true

[dev-dependencies]
rustscale-tailcfg = { path = "../tailcfg", version = "0.1.0" }
