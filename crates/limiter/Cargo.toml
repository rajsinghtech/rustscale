[package]
name = "rustscale-limiter"
description = "Token-bucket rate limiter (keyed with LRU and single-bucket)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
